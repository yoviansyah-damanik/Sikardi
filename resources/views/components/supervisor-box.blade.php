<div class="inline-block w-full text-center border max-w-80">
    <div class="px-3 py-2 font-bold text-white bg-primary-700 dark:bg-slate-800">
        {{ $supervisor['name'] ?? '-' }}
    </div>
    <div class="px-3 py-2 font-semibold bg-white dark:bg-slate-700">
        NIDN. {{ $supervisor['nidn'] ?? '-' }}
    </div>
</div>
